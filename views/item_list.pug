extends layout

block content
  h1= title
  if item_list.length
    ul
      each item in item_list
        li
          a(href=item.url) #{item.name}
          each cat in item.category
            |  (#{cat.name})

  else
    p There are no items.
